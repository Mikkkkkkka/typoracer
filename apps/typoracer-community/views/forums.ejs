<main class="page-content">
    <section class="page-section forums-section">
        <div class="section-intro">
            <h1>Forums</h1>
            <p>Open a discussion to read the full text and replies.</p>
            <% if (currentUser) { %>
                <p><a href="/forums/new">Start a discussion</a></p>
            <% } %>
            <% if (discussionCreated) { %>
                <p class="generic-form__success">Discussion created.</p>
            <% } %>
        </div>
        <div class="forums-list">
            <% discussions.forEach((discussion) => { %>
                <article class="card forum-thread">
                    <p class="forum-thread__meta">Started by <a href="/users/<%= discussion.author %>"><%= discussion.author %></a></p>
                    <h2 class="forum-thread__title"><a class="card-link" href="/forums/<%= discussion.id %>"><%= discussion.title %></a></h2>
                    <p class="forum-thread__excerpt"><a class="card-link" href="/forums/<%= discussion.id %>"><%= discussion.excerpt %></a></p>
                </article>
            <% }) %>
        </div>
    </section>
</main>
